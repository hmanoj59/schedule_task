%nav.navbar.navbar-full.navbar-dark.bg-primary
  = link_to ENV['APPNAME'], root_path, class: "navbar-brand"
  %ul.nav.navbar-nav
    - unless current_user
      %li.nav-item
        = link_to 'About', page_path('about'), class: 'nav-link'
    - else
      %li.nav-item.active
        = link_to 'Dashboard', dashboard_index_path, class: 'nav-link'
        %li.nav-item.active
          = link_to 'schedules', schedules_path, class: 'nav-link'
          %li.nav-item.active
            = link_to 'Cloud account', cloud_accounts_index_path, class: 'nav-link'
        %span.sr-only (current)
  %ul.nav.navbar-nav.pull-xs-right
    - if current_user
      %li.nav-item
        = link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link'
    - else
      %li.nav-item
        = link_to 'Login', new_user_session_path, class: 'nav-link'
      %li.nav-item
        = link_to 'Sign Up', new_user_registration_path, class: 'nav-link'
